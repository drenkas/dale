interface:
  display_name: "Dale Graph"
  short_description: "Fork and route nested Codex tasks"
  default_prompt: "Use $dale-graph to fork the current task into an adaptive GPT-5.6 graph whose nodes may orchestrate bounded subagents."
